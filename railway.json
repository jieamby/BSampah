{
  "$schema": "https://railway.app/railway-schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --include=dev && ./node_modules/.bin/nest build"
  },
  "deploy": {
    "startCommand": "node dist/main"
  }
}
